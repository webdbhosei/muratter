<%= form_with(model: chat, local: true) do |form| %>
  <% if chat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(chat.errors.count, "error") %> prohibited this chat from being saved:</h2>

      <ul>
      <% chat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content, :size => "60" %>
  </div>

  <div class="field">
    <%= form.label :chatroom_id %>
    <%= form.select :chatroom_id,
        Chatroom.all.collect{|c| [c.name, c.id]} %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
